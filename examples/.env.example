# OpenProject Configuration for surveilr Integration
# Copy this file to .env and fill in your credentials

# Required: Your OpenProject API key
# Get this from: My Account → Access tokens → + API
OPENPROJECT_API_KEY=your-api-key-here

# Required: Base URL of your OpenProject instance
# Include /api/v3 at the end
OPENPROJECT_BASE_URL=https://your-instance.openproject.com/api/v3

# Optional: Request timeout in seconds (default: 30)
OPENPROJECT_TIMEOUT=30

# Optional: Maximum number of retry attempts (default: 3)
OPENPROJECT_MAX_RETRIES=3

# Optional: Filter projects updated after this date
# OPENPROJECT_START_DATE=2024-01-01T00:00:00Z
